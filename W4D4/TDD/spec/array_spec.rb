require "rspec"
require "array"

describe Array do 
    let(:my_arr) {[1, 2, 1, 3, 3]}
    describe "#my_uniq" do 
        it "removes duplicates" do
          my_arr.each do |ele| 
            expect(my_arr.my_uniq
            expect(my_arr).to receive(my_uniq).and_return([1,2,3]) 
                
        end 
    end 
end 

# describe "#[]" do
#   it "should accept an index as an arg" do
#     code[1]
#   end

#   it "should return the element of @pegs at the given index" do
#     expect(code[1]).to eq("G")
#     expect(code[2]).to eq("R")
#   end
# end